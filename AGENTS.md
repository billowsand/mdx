# AGENTS.md

## 版本号迭代规则

每次修订代码后，必须同步迭代 `Cargo.toml` 中的 `version`，按语义化版本（SemVer）依据修订性质选择：

- **修订性质 → 版本位**
  - 修复 bug、小改动、不影响既有行为：patch 位 +1（如 1.0.0 → 1.0.1）
  - 新增功能、向后兼容的行为变化：minor 位 +1（如 1.0.0 → 1.1.0）
  - 破坏性变更（输出结构、命令行接口、既有行为不兼容）：major 位 +1（如 1.0.0 → 2.0.0）
- 修改版本号后运行 `cargo check`（或构建/测试）让 `Cargo.lock` 同步更新。
- 若已通过 `cargo install --path .` 安装本工具，版本号变化后应重新安装，保证 `mdx --version` 与源码一致。
