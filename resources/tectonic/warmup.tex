\documentclass[fontset=fandol]{ctexbook}

% Keep this package list in sync with resources/official/official.cls and
% resources/research/md2tex.cls. The release workflow compiles this document
% once against Tectonic's official online bundle, then packages the resolved
% files as mdx's local, offline directory bundle.
\usepackage{geometry}
\usepackage{calc}
\usepackage{fancyhdr}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{array}
\usepackage{pifont}
\usepackage{refcount}
\usepackage{ifthen}
\usepackage{ragged2e}
\usepackage{paralist}
\usepackage{longtable}
\usepackage{etoolbox}
\usepackage{pdflscape}
\usepackage{float}
\usepackage{caption}
\usepackage{tabularray}
\usepackage{tikz}
\usepackage{titlesec}
\usepackage{tocloft}
\usepackage{listings}
% 数学公式支持（与 resources/research/md2tex.cls 保持同步）
\usepackage{amsmath}
\usepackage{mathtools}
\usepackage{gbt7714}
\citestyle{numbers}
\usepackage[unicode,CJKbookmarks=true]{hyperref}

% Some resources are requested lazily by configured listings/list labels or by
% xdvipdfmx, so package loading alone does not resolve them during prewarming.
\IfFileExists{color.sty}{}{\PackageError{mdx-warmup}{Missing color.sty}{}}
\IfFileExists{lcircle1.pfb}{}{\PackageError{mdx-warmup}{Missing lcircle1.pfb}{}}

\begin{document}
\tableofcontents
\chapter{离线依赖预热}

中文、English、\ding{72}，参考文献\cite{warmup2026}。

{\fontsize{14.05249pt}{24pt}\selectfont 大字号脚注\footnote{离线脚注资源。}}

\section{标题资源}
\subsection{次级标题资源}

\begin{longtblr}{colspec={X[l]X[c]},hlines,vlines}
组件 & 状态 \\
Tectonic & 可用
\end{longtblr}

\begin{tikzpicture}
  \draw (0,0) rectangle (1,1);
\end{tikzpicture}

\begin{lstlisting}[language=C]
fn main() { println!("offline"); }
\end{lstlisting}

% 行内与独立公式：让数学字体与度量资源在预热时解析进 bundle。
% 注意 xdvipdfmx 需要的是 Type1 实体字体（.pfb），只有真正排过 PDF 的字号/字族
% 组合才会被解析进缓存；研究正文是 14.05249bp，数学落在 cmr12/cmmi12 等设计
% 尺寸上，与默认 10pt 预热出的 .pfb 不是同一批文件，必须按实际字号排一遍。
行内公式 $E=mc^2$ 与独立公式：
\[
  \int_0^1 x^2\,dx = \frac{1}{3}
\]

{\fontsize{14.05249pt}{24pt}\selectfont
研究正文字号下的行内公式 $E=mc^2$、$\sum_{i=1}^{n} i = \frac{n(n+1)}{2}$、
$a \leq b \cdot c \times d \pm e \neq f \to \infty$、$\alpha + \beta \geq \gamma$、
$\sqrt{x^2+y^2}$ 与 $\lim_{x \to 0} \frac{\sin x}{x} = 1$。
\[
  \begin{pmatrix} a & b \\ c & d \end{pmatrix}
  \begin{cases} x + y = 1 \\ x - y = 2 \end{cases}
  \qquad
  \begin{aligned} u &= v + w \\ &= z \end{aligned}
\]}

\begin{asparaenum}
  \item 一级列表
  \begin{inparaenum}
    \item 二级列表
  \end{inparaenum}
\end{asparaenum}

\begin{landscape}
横向页面。
\end{landscape}

\bibliography{warmup}
\end{document}
