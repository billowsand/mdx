# 更新日志

本项目的所有重要变更都会记录在此文件中，格式参考
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [2.8.0] - 2026-07-30

### 变更

- 省略 `-o` 时，TeX 输出不再散落在当前目录，而是连同 `data/`、`appendix/`、
  `figures/`、`.cls`、`.bib` 与 PDF 一并收进一个单独目录：单文件输入取文件名
  （`report.md` → `report/report.tex`），目录输入取目录名加 `-tex` 后缀以避免
  与输入目录冲突（`chapters/` → `chapters-tex/chapters.tex`）。docx 是自包含
  单文件，仍直接生成在当前目录。GUI 的默认输出路径同步采用该布局，且切换
  输出格式时按新格式整体重算路径（不再只改扩展名）。

## [2.7.3] - 2026-07-30

### 变更

- official tex 列表圈号字体由 Segoe UI Symbol 改为跟随正文仿宋
  （与 research 用正文主字体 FZShuSong-Z01 同一思路），圈号与正文字形
  风格一致。已知限制：中文字体普遍缺 ⑪-⑳，L1 列表超过 10 项会缺字
  （research 亦同）。

## [2.7.2] - 2026-07-30

### 变更

- official tex 列表圈号与 research 对齐：①-⑳、⑴-⒇ 改用字体自带的专门字符
  （不再用 `\textcircled` 现场画圈）。圈号经 xeCJK 路由到罗马字体，实测中文
  字体（SimSun/仿宋/楷体/黑体/微软雅黑/FZShuSong-Z01）均缺 ⑪-⑳，故首选
  Segoe UI Symbol（全覆盖），非 Windows 环境回退宋体系并告警。

## [2.7.1] - 2026-07-30

### 变更

- official tex 表格字号由小四（12bp）上调为四号（14bp），行距 21pt 与正文
  1.5 倍一致；表题编号与题文同步改为四号（黑体/仿宋）。

## [2.7.0] - 2026-07-30

### 变更

- official tex 表格与 research 看齐：由 `longtable` 改为 `longtblr`（tabularray）
  智能表格，自动分析内容决定对齐方式与列宽比例，窄数字列（序号列一类）固定
  `2em` 列宽，表头黑体居中并在续页重复，续页带"（续表）/下一页继续"提示。
- official tex 支持表题（caption）：`表：标题` / `: 标题` 等写法解析后由
  LaTeX 计数器自动编号输出（"表 1  标题"，黑体编号 + 仿宋题文），不再被丢弃。
- official tex 支持表格锚点：`表：标题 {#tbl:id}` 定义后可在正文中用
  `{@tbl:id}` 引用（`\ref`）；章节锚点仍因无自动编号而报硬错误。
- `official.cls` 新增 `caption`、`tabularray` 宏包及 longtblr 表内字体
  （小四仿宋、黑体表头）与表题样式配置。

## [2.6.0] - 2026-07-29

### 新增

- 新增基于 egui/eframe 的 `mdx-gui` 桌面程序，支持选择或拖入 Markdown 文件及目录、
  选择 DOCX/TeX 与公文/研究报告组合、自定义研究报告 LaTeX 模板、可选 PDF 编译、
  后台转换、任务日志以及打开输出位置。
- GUI 内嵌 `font/sfss.ttf` 作为中文界面字体。
- 新增可供 CLI 与 GUI 共用的转换库接口、结构化请求/结果和进度事件。

### 变更

- `mdx` CLI 改为调用公共转换接口，命令行参数及既有转换行为保持不变。
- GUI 依赖由可选的 `gui` feature 管理；普通 CLI 构建不需要编译桌面图形依赖。

## [2.5.1] - 2026-07-28

### 新增

- front matter 新增 `年限`（别名 `保密年限`/`保密期限`/`years`）字段，与 `密级`
  分列两个变量；research 封面以五角星连接排为 `密级：机密★5年`，tex 由 pifont
  的 `\ding{72}` 输出。未填 `年限` 时不出现星号。

### 变更

- research tex 表格自动列宽：表头以外全为 2 位以内纯数字的列（序号列一类）
  改用固定 `2em` 列宽，不再参与按比例伸缩；其余列继续按原比例算法分配。

## [2.5.0] - 2026-07-28

### 变更

- tex 输出（official 与 research）改用紧缩排版：页面按内容自然收尾（`\raggedbottom`），
  不再为撑满版心而拉伸段落间距；浮动体与表格前后留白收紧。
- 图片浮动体位置由 `[htbp]` 改为 `[H]`，就地排版在正文指定位置，不再漂移。
- official 表格由 `tabular` 改为 `longtable`，可跨页断开并在续页重复表头，
  避免为"整表完整显示"把表整体推到下一页而留下大片空白。

### 修复

- tex 列表环境结束后补空行，避免其后的正文被并入列表最后一项所在段落，
  导致后续段落缩进与段距错乱。

## [2.4.0] - 2026-07-26

### 新增

- 面向 GitHub 的项目首页、贡献指南、行为准则、安全策略与 Issue/PR 模板。
- 跨平台持续集成，以及由 `v*` 标签触发的多平台二进制发布工作流。
- Cargo 包元数据、MIT 许可证和自动发布说明分类；明确关闭 crates.io 发布，避免与现有同名 crate 冲突。
- GitHub 社交媒体预览图（`docs/assets/social-preview.png`）及可编辑 SVG 源文件。

### 变更

- 将 `Cargo.lock` 纳入版本控制，以保证 CLI 应用构建可复现。
- 开放跟踪用户语法文档与精选示例，让用户能直接从仓库开始体验。

## [2.3.0] - 2026-07-25

### 新增

- 加粗和斜体内容可嵌套链接、图片、交叉引用、代码及文献引用。

### 修复

- TeX PDF 编译优先使用 XeLaTeX，并在不可用时回退到 Tectonic。
- DOCX 输出会自动创建目标目录。
